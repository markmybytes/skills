# STYLE.md Review — install-it workspace

Audit mode: read-only findings against the STYLE.md opinionated structure. No files modified.

## Verdict

Structure is fundamentally sound: rule priority order is present and correct, every section maps to a real repo layer (Vue/TS frontend + Go backend), no dead sections, no empty placeholders, and the layout facts check out against source. Main problems are **operational facts duplicated from AGENTS.md** and **lint-enforceable rules restated instead of pointing at tool config**.

## Findings

**STYLE.md — operational facts duplicated from AGENTS.md (owner of commands, tests, boundaries, repo map)** (`duplicate`)

- `STYLE.md:44` — "Do not hand-edit `frontend/wailsjs/**` — it is auto-generated by Wails" duplicates `AGENTS.md:39` → replace with a link to AGENTS.md.
- `STYLE.md:46` — "Run `npm run lint` and `npm run format` before manually fixing style issues" duplicates `AGENTS.md:28-29` (Commands) → replace with a link to AGENTS.md Commands.
- `STYLE.md:228` — "New Go structs exposed to the frontend must be added to `Bind` in `main.go`. Enums go in `EnumBind`" duplicates `AGENTS.md:76` verbatim → replace with a link to AGENTS.md.
- `STYLE.md:224` — "`Porter` closes and reopens the DB on import — if adding storage code, account for this lifecycle" duplicates `AGENTS.md:75` → replace with a link to AGENTS.md.
- `STYLE.md:222` — "Use `glebarez/sqlite` (CGO-free). Do not introduce CGO-dependent drivers" duplicates `AGENTS.md:40` and `AGENTS.md:72` → replace with a link to AGENTS.md.
- `STYLE.md:250` — Quick Review Checklist restates "`go fmt` / `go vet` applied (Go); `npm run lint` / `npm run format` pass (Vue/TS)", the same commands listed in AGENTS.md → point to AGENTS.md Commands.

**STYLE.md — mechanical, lint-enforceable rules restated instead of pointing at tool config** (`structure`)

- `STYLE.md:54-57` — Frontend "Basics" bullets (block order `<script>` → `<template>` → `<style>`, no Options API, type-based props/emits, blank lines between template tags) are all enforced by `frontend/eslint.config.js` (`vue/block-order`, `vue/component-api-style`, `vue/define-props-declaration`, `vue/define-emits-declaration`, `vue/padding-line-between-tags`) → collapse to a single pointer at the ESLint config and keep only non-enforceable judgment rules.
- `STYLE.md:57` — "Blank lines between all template tags" even cites the ESLint rule name (`vue/padding-line-between-tags`); the config already sets it to `error` → redundant, drop the bullet.

**STYLE.md — ownership declaration** (`gap`)

- `STYLE.md:3` — Line 3 declares what is canonical here ("Canonical conventions for install-it…") but never states what is owned elsewhere: commands/tests/boundaries → `AGENTS.md`, setup/onboarding → `README.md`. "Related Docs" links exist but carry no ownership statement → add one sentence naming the other owners.

## Confirmed good (verified against repo evidence)

- Rule priority order present and matches the recommended default: Security → Correctness → Architecture → Simplicity → Local consistency (`STYLE.md:11-17`).
- No dead sections or empty rule bullets: both language layers exist (Vue 3 frontend in `frontend/`, Go backend in `pkg/`), all sections populated.
- Facts check out: window min `640x480` and default `768x576` match `main.go:116-121`; `glebarez/sqlite` and `gormigrate/v2` in `go.mod`; migrations live in `pkg/storage/db.go`; `@nuxt/ui` v4 + Tailwind v4 in `frontend/package.json`.
- i18n prefix table and naming tables are conventions (not facts), appropriate for STYLE.md.
- "Layout Breakpoints" section is a genuine style rule (skip `sm:`/`lg:`), well-justified with its own rationale — not lint-enforceable.

## Items needing user-provided facts

None. All claims verified against repository evidence; no unknowns.

## Files changed

None — audit mode, review only. Report only, as requested.